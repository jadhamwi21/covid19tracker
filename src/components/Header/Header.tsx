import React from "react";
import { HeaderContainer } from "./Header.sc";

type Props = {};

const Header = (props: Props) => {
	return <HeaderContainer>Covid-19 Tracker</HeaderContainer>;
};

export default Header;
